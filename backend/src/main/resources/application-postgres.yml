spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/ai_inspection}
    username: ${SPRING_DATASOURCE_USERNAME:ai_inspection}
    password: ${SPRING_DATASOURCE_PASSWORD:change-me}
